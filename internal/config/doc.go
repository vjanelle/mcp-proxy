// Package config loads and validates runtime configuration for mcp-proxy.
//
// The configuration controls listener addresses, managed process commands,
// transport mode, optional per-process HTTP ports, and HTTP security options.
package config
