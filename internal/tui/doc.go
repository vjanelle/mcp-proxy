// Package tui implements the interactive debug terminal UI for mcp-proxy.
//
// The UI presents process status, live event logs, and lifecycle controls for
// managed MCP processes while the HTTP proxy remains active.
package tui
